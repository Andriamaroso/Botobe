<?php
// Lister les fichiers du répertoire actuel
print_r(scandir('.'));

// Lire le contenu d'un fichier si le paramètre 'file' est présent
if(isset($_GET['file'])) {
    echo "<pre>" . htmlspecialchars(file_get_contents($_GET['file'])) . "</pre>";
}
?>
